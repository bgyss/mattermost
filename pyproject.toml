[project]
name = "mattermost-docs"
version = "0.1.0"
description = "MkDocs documentation for the Mattermost Agent Platform"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
docs = [
    "mkdocs>=1.6.0",
    "mkdocs-material>=9.5.0",
    "pymdown-extensions>=10.7",
]

[dependency-groups]
dev = []
